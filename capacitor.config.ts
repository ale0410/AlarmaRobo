import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.app3',
  appName: 'alarma-robo-2023',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
